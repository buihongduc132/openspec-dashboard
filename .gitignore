# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build outputs
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Test coverage
coverage/

# Environment & secrets (NEVER commit)
.env
.env.*
!.env.example
!.env.template
*.env
env.local
*.local

# Keys, certs, credentials
*.key
*.pem
*.crt
*.p12
*.pfx
secrets/
secrets.json
auth.json
credentials.json
service-account*.json

# Local config overrides (may carry connection strings / API keys)
config.local.yaml
config.local.yml

# Dashboard-private metadata (contains user/project data)
openspec/.dashboard/
**/.dashboard/
# §8.9 fallback location (pre-ignored so a gate-flip never leaks PII)
.openspec-dashboard/
**/.openspec-dashboard/

# Database / runtime state
*.db
*.db-journal
*.sqlite
*.sqlite3
data/
.db/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.swp
*.swo
*~

# Drizzle
drizzle/migrations/meta/
